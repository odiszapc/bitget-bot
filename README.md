# Bitget Short Bot 🐻

Автоматический бот для торговли шортами на фьючерсах Bitget.

## Быстрый старт

### 1. Установи Python 3.10+

Проверь:
```bash
python3 --version
```

### 2. Установи зависимости

```bash
pip install -r requirements.txt
```

### 3. Настрой API ключи

Открой `config.json` и заполни:
```json
{
  "api_key": "ТВОЙ_КЛЮЧ",
  "api_secret": "ТВОЙ_СЕКРЕТ",
  "passphrase": "ТВОЙ_ПАРОЛЬ"
}
```

Как получить ключи на Bitget:
1. Зайди в профиль → API Management
2. Создай новый API ключ
3. Включи разрешения: Read + Trade (для фьючерсов)
4. **НЕ включай** разрешение на вывод средств!

### 4. Начни с демо-режима

В `config.json` убедись что `"demo": true`. Это использует тестовый аккаунт Bitget — реальные деньги не затронуты.

```bash
python bot.py
```

### 5. Тестовый прогон без ордеров

```bash
python bot.py --dry-run
```

Бот будет анализировать рынок, находить сигналы, но не будет открывать реальные позиции. Все действия пишутся в лог.

### 6. Боевой режим

Когда убедишься что всё работает:
1. Поставь `"demo": false` в config.json
2. Запусти: `python bot.py`

## Настройки (config.json)

| Параметр | По умолчанию | Описание |
|----------|-------------|----------|
| `leverage` | 10 | Плечо |
| `max_positions` | 5 | Макс. открытых позиций |
| `timeframe` | "15m" | Таймфрейм свечей |
| `min_volume_usd` | 5000000 | Мин. объём 24ч (USD) |
| `max_atr_pct` | 15.0 | Макс. ATR в % (фильтр волатильности) |
| `min_stop_pct` | 2.0 | Мин. стоп-лосс в % |
| `min_tp_pct` | 5.0 | Мин. тейк-профит в % |
| `daily_loss_limit_pct` | 5.0 | Дневной лимит убытков в % |
| `btc_bull_limit_pct` | 5.0 | Макс. рост BTC за 24ч |
| `cycle_minutes` | 15 | Интервал между циклами |
| `trailing_start_pct` | 3.0 | Начало трейлинга при % прибыли |
| `trailing_distance_pct` | 2.0 | Дистанция трейлинг-стопа |
| `min_signals` | 3 | Мин. кол-во сигналов для входа |

## Файлы

- `bot.py` — главный файл, запуск бота
- `exchange.py` — работа с API Bitget
- `strategy.py` — индикаторы и сигналы
- `risk.py` — риск-менеджмент и защита
- `state.py` — сохранение состояния
- `config.json` — настройки
- `state.json` — автоматический файл состояния (не редактируй)
- `logs/` — логи по дням

## Доработка через Claude Code

Проект содержит `CLAUDE.md` с полным описанием стратегии. Для доработки:

```bash
cd bitget-short-bot
claude
```

Claude Code прочитает CLAUDE.md и будет понимать весь контекст проекта.

## ⚠️ Важно

- **Это не гарантия прибыли.** Любая торговля — это риск.
- **Начни с демо.** Убедись что бот работает корректно.
- **Не вкладывай больше, чем готов потерять.**
- **Следи за ботом.** Автоматика не заменяет здравый смысл.
